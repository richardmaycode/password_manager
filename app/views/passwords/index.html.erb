
<%= tag.div class: "flex justify-between items-center" do %>
  <%= tag.h1 "All Passwords", class: "mb-0" %>
  <%= link_to "New Password", new_password_url %>
<% end %>
<%= tag.div (render @passwords), class: "flex flex-col divide-y divide-gray-100" %>