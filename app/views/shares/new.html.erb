<%= link_to "← Back", password_path(@password), class: "block mb-4 no-underline text-sm" %>
<%= tag.h1 "Share Password" %>

<%= render "form", 
    password: @password, 
    user_password: @user_password
%>